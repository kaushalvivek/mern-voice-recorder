import React, { Component } from 'react';
import { Link } from 'react-router-dom';

export default class Pending extends Component {

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div className="container">
        Pending would be implemented here.
      </div>
    );
  }
}